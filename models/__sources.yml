version: 2

sources:
  - name: raw_data
    schema: "ticktick_dev"
    description: raw tasks data from ticktick api
    tables:
      - name: tasks
        description: raw tasks data from tickpy
        # meta:
        #   dagster:
        #     asset_key : ["tasks_raw"]z

      - name: projects
        description: raw project data from ticktick api
        # meta:
        #   dagster:
        #     asset_key : ["projects_raw"]


  - name: gcs_snapshot
    tables:
     - name: tasks
       config:
        external_location: "s3://ticktick_raw/dev/raw_tasks.csv"
       
     - name: projects
       config:
         external_location: "s3://ticktick_raw/dev/raw_projects.csv"